<div class="container mx-auto mt-12">
  <h2 class="text-center text-2xl font-bold text-blue-600">Order Confirmation</h2>

  <div class="alert alert-info text-center mt-6 p-4 border border-blue-300 bg-blue-50 rounded-md">
    <strong class="text-lg">Total Price:</strong> ₹<%= @order.total_price %>
  </div>

  <p class="text-center mt-4 text-lg">Do you want to confirm this order?</p>

  <div class="flex justify-center mt-6 space-x-4">
    <a href="<%= confirm_order_path(@order) %>" class="bg-green-500 text-white px-6 py-3 rounded-full hover:bg-green-600 transition-all">Confirm Order</a>
    <a href="<%= new_order_path %>" class="bg-gray-500 text-white px-6 py-3 rounded-full hover:bg-gray-600 transition-all">Cancel</a>
  </div>
</div>
